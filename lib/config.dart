/// ============================================================
///  STUDY BUDDY — BACKEND CONFIGURATION
///  This is the ONE place you need to edit to point the app at your
///  deployed Vercel backend (the same one already serving api/chat.js
///  and api/status.js).
/// ============================================================
///
/// Set this to your Vercel deployment's URL, with no trailing slash.
/// Example: 'https://study-buddy-yourname.vercel.app'
///
/// The AI Buddy and the "AI Buddy" status row in Settings both call this
/// URL's /api/chat and /api/status endpoints. No API key lives in this
/// app anywhere — the key stays on that backend, exactly as before.
const String backendBaseUrl = 'study-buddy7-git-main-salma2m2k-8926s-projects.vercel.app';
